package dev.isxander.classicplayground;

public interface PlayerHolder {
    void setDescending(boolean descending);
    boolean getDescending();

    float getNoClipSpeed();
    void setNoClipSpeed(float noClipSpeed);
}
